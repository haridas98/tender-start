services:
  tender-start-web:
    image: nginx:1.27-alpine
    container_name: tender-start-web
    restart: unless-stopped
    ports:
      - "${TENDER_START_PORT:-16108}:80"
    volumes:
      - ./dist:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
